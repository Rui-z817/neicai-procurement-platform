# 2026-09-02 重装系统后恢复验证与补全

## 背景
用户重装 Windows（旧用户名 Administrator → 新用户名 FanFan），询问恢复状态。

## 恢复前已就绪（用户/系统自行完成）
- 线上服务：GitHub Pages HTTP 200、服务器 API health OK、neicai-api active、DB 32 条内部价格、uploads 报价单在
- 本地：app/ 代码（= backup 分支快照，比重装前远端 main 还新，含 sourceType/fileId 等未推送改动）、backup_local/（credentials.txt + server_data: data.db/export_20260902.json/uploads）
- WorkBuddy 自动化「南京信息价每月自动更新」仍 ACTIVE（云端同步）
- GitHub Actions 云端每月更新 8/1 success、pages build 8/28 success

## 本次补全的缺口
1. **SSH 私钥**：从 backup 分支 ops/ 下载 neicai_deploy_key(.pub) → ~/.ssh/ + .workbuddy/ 两处，chmod 600，写 ~/.ssh/config（Host neicai-server），SSH 免密连接验证成功
2. **ops/ 运维脚本**：RESTORE_GUIDE.md、deploy_ghpages.cjs、deploy_to_lighthouse.py、restore_data_to_server.py → .workbuddy/ops/
3. **.workbuddy/memory/**：4 个记忆文件（06-26/06-29/08-28/08-29）+ server/api-server.js + package.json → 从 backup 分支恢复
4. **自动化任务路径修复（关键）**：prompt 内 5 处 C:\Users\Administrator\ → C:\Users\FanFan\，cwds 同步更新，否则每月 3 日必失败
5. **Python venv 重建**：.workbuddy/binaries/python/envs/default + pdfplumber 0.11.10 + requests（parse_pdf.py 依赖）
6. **npm install**：npmmirror，291 包，tsc --noEmit exit 0
7. 补回本地缺失的 2 个 workflow 文件（.github/workflows/deploy-pages.yml、update-prices.yml——与远端 main 对齐）

## 遗留事项
- 本地 git HEAD (94a3ee4) 落后远端 main (6990f0d)，但**本地工作区更新**（backup 快照）。git fetch 直连被墙（schannel/openssl 均失败，curl API 可通），下次开加速器后建议推送本地最新代码到 main
- 线上部署已是最新（重装前部署过），无需重新部署
